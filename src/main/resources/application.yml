server:
  port: 8082

spring:
  application:
    name: orderservice
  data:
    mongodb:
      uri: mongodb://localhost:27017/ordenes
  cloud:
    aws:
      region:
        static: us-east-1
      credentials:
        access-key: test
        secret-key: test
      cloudwatch:
        endpoint: http://localhost:4566
      logging:
        log-group: practica-microservicios
        log-stream: ordenes-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
